using System;
//zad1
/*class Arr{
  public int binarySearch(int[] arr, int l, int r,int x){
    if (r>=l){
      int middle = l + (r-l)/2;
      if (arr[middle]==x)
        return middle;
      if (arr[middle]>x)
        return binarySearch (arr, l , middle-1, x);
      return binarySearch(arr, middle+1, r, x);
    }
    return -1;
  }
}*/



class Program {
  public static void Main (string[] args) {
    //zad1
    /*int[] arr = {1, 3, 4, 7, 10, 14};
    int n = arr.Length;
    int x = 10;
    Arr myArray = new Arr();
    int result = myArray.binarySearch(arr, 0, n-1, x);

    if (result == -1){
      Console.WriteLine("nqma takuw element");
    } else{
      Console.WriteLine("elementa e " + result);
    }*/

    //zad2
    double t = Convert.ToInt32(Console.ReadLine());
    double izraz = Math.Pow(t, 3)+5;
    double dolu;
    double gore;
    if (t>dolu && t<gore){
    double middle = (a+b)/2;
    while (middle!=0){
      
    }
      }
  }
}